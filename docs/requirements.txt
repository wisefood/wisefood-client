sphinx>=7.0
furo>=2024.1.29
myst-parser>=2.0
sphinx-copybutton>=0.5
sphinx-design>=0.5
# Install the package itself so autodoc can import `wisefood`.
# Read the Docs runs `pip install -r docs/requirements.txt` from the repo root,
# so this path is resolved relative to that root.
-e .
